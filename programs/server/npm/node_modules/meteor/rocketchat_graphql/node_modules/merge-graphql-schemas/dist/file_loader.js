'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});

var _fs = require('fs');

var _fs2 = _interopRequireDefault(_fs);

var _path = require('path');

var _path2 = _interopRequireDefault(_path);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

var recursiveReadDirSync = function recursiveReadDirSync(dir) {
  return _fs2.default.readdirSync(dir).reduce(function (files, file) {
    return _fs2.default.statSync(_path2.default.join(dir, file)).isDirectory() ? files.concat(recursiveReadDirSync(_path2.default.join(dir, file))) : files.concat(_path2.default.join(dir, file));
  }, []);
};

var readDirSync = function readDirSync(dir) {
  return _fs2.default.readdirSync(dir).reduce(function (files, file) {
    return _fs2.default.statSync(_path2.default.join(dir, file)).isDirectory() ? files : files.concat(_path2.default.join(dir, file));
  }, []);
};

var fileLoader = function fileLoader(folderPath) {
  var options = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : { recursive: false };

  var dir = folderPath;
  var files = [];
  var schemafiles = options.recursive === true ? recursiveReadDirSync(dir) : readDirSync(dir);

  schemafiles.forEach(function (f) {
    var pathObj = _path2.default.parse(f);

    if (pathObj.name.toLowerCase() === 'index') {
      return;
    }

    switch (pathObj.ext) {
      case '.js':
        {
          var file = require(f); // eslint-disable-line
          files.push(file.default || file);
          break;
        }

      case '.graphqls':
      case '.gql':
      case '.graphql':
        {
          var _file = _fs2.default.readFileSync(f, 'utf8');
          files.push(_file.toString());
          break;
        }

      default:
    }
  });
  return files;
};

exports.default = fileLoader;